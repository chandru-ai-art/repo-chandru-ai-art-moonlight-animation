<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Royal Hologram Proposal</title>
<style>
:root { --pulse:1.2; --time:2s; }
body { margin:0; background:#000; overflow:hidden; font-family:sans-serif; }
.scene { position:absolute; inset:0; display:flex; justify-content:center; align-items:center; transition:opacity 1s; }
.hidden { opacity:0; pointer-events:none; }
.holo { fill:rgba(0,255,255,.15); stroke:cyan; stroke-width:2; filter:drop-shadow(0 0 6px cyan); animation:flicker var(--time) infinite alternate; }
@keyframes flicker { 50%{stroke:#0ff; filter:drop-shadow(0 0 10px #0ff);} }
.pulse { animation:pulse var(--time) infinite; }
@keyframes pulse { 50%{transform:scale(var(--pulse));} }
.btn { position:absolute; bottom:15%; padding:12px 24px; background:#ff3b8d; color:#fff; border:none; border-radius:6px; font-size:18px; cursor:pointer; animation:pulse var(--time) infinite; }
.moon { position:absolute; top:10%; right:15%; width:120px; height:120px; border-radius:50%; background:radial-gradient(circle at 40% 35%,#fff 0%,#d6e6ff 40%,#aac8ff 60%,transparent 76%); box-shadow:0 0 40px #a6c7ff55; animation:moonGlow 4s ease-in-out infinite alternate; }
@keyframes moonGlow { to{box-shadow:0 0 60px #a6c7ffcc;} }
/* Scanlines overlay */
.scanlines { position:absolute; inset:0; background:repeating-linear-gradient(to bottom,rgba(0,255,255,0.08) 0px,rgba(0,255,255,0.08) 2px,transparent 2px,transparent 4px); mix-blend-mode:screen; animation:scanMove 3s linear infinite; pointer-events:none; }
@keyframes scanMove { to{background-position:0 100%;} }
/* Particle dissolve */
.dissolve { animation:dissolve 1s forwards; }
@keyframes dissolve { to{opacity:0; filter:blur(10px);} }
/* Materialize from particles */
.materialize { opacity:0; animation:materialize 1.2s forwards; }
@keyframes materialize {
  0% { opacity:0; filter:blur(12px) brightness(2); transform:scale(1.2); }
  50% { opacity:0.5; filter:blur(6px) brightness(1.5); }
  100% { opacity:1; filter:blur(0) brightness(1); transform:scale(1); }
}
</style>
</head>
<body>

<div class="scanlines"></div>

<svg style="display:none">
  <defs>
    <g id="char"><circle r="15"/><rect y="15" x="-5" width="10" height="40"/></g>
    <g id="crown"><path d="M-10 -15 L-5 -25 L0 -15 L5 -25 L10 -15 Z"/></g>
    <g id="tiara"><path d="M-10 -15 Q0 -25 10 -15"/></g>
    <g id="tower"><path d="M0 -100 L-20 0 L20 0 Z M-20 0 L-50 150 L50 150 L20 0 Z"/></g>
    <g id="bridge"><rect x="-400" y="0" width="800" height="20"/></g>
  </defs>
</svg>

<!-- Scene 1 -->
<div class="scene" id="s1">
  <svg viewBox="0 0 800 400">
    <rect width="800" height="400" fill="url(#grad)"/>
    <use href="#tower" transform="translate(400,150)" fill="#222"/>
    <use href="#bridge" transform="translate(400,300)" fill="#444"/>
    <g class="holo materialize">
      <use href="#char" transform="translate(370,260)"/>
      <use href="#crown" transform="translate(370,245)"/>
      <use href="#char" transform="translate(430,260)"/>
      <use href="#tiara" transform="translate(430,245)"/>
    </g>
    <defs><linearGradient id="grad" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stop-color="#1a2a6c"/><stop offset="100%" stop-color="#b21f1f"/></linearGradient></defs>
  </svg>
  <div class="pulse" style="font-size:40px;cursor:pointer;" onclick="next(2)">💍</div>
</div>

<!-- Scene 2 -->
<div class="scene hidden" id="s2">
  <svg viewBox="0 0 800 400">
    <rect width="800" height="400" fill="#1a1a2e"/>
    <g class="holo materialize">
      <use href="#char" transform="translate(300,200)"/>
      <use href="#crown" transform="translate(300,185)"/>
      <circle cx="345" cy="230" r="5" fill="gold"/>
      <use href="#char" transform="translate(400,180)"/>
      <use href="#tiara" transform="translate(400,165)"/>
    </g>
  </svg>
  <button class="btn" onclick="next(3)">Accept ❤️</button>
</div>

<!-- Scene 3 -->
<div class="scene hidden" id="s3">
  <div class="moon"></div>
  <svg viewBox="0 0 800 400">
    <rect width="800" height="400" fill="#000"/>
    <g class="holo materialize">
      <use href="#char" transform="translate(380,200)"/>
      <use href="#char" transform="translate(420,200)"/>
    </g>
  </svg>
</div>

<script>
function next(n){
  const current = document.querySelector('.scene:not(.hidden)');
  current.classList.add('dissolve');
  setTimeout(()=>{
    document.querySelectorAll('.scene').forEach(s=>s.classList.add('hidden'));
    const nextScene = document.getElementById('s'+n);
    nextScene.classList.remove('hidden','dissolve');
    // retrigger materialize animation
    nextScene.querySelectorAll('.materialize').forEach(el=>{
      el.style.animation='none';
      el.offsetHeight; // force reflow
      el.style.animation='';
    });
  },1000);
}
</script>

</body>
</html>